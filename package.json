{
  "name": "heritage-restored",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build:bible": "node ./scripts/build-bible-cited.mjs",
    "seed:bsb": "node ./bsb-data-pipeline/scripts/prepare-bsb.mjs",
    "predev": "npm run build:bible",
    "prebuild": "npm run build:bible",
    "check": "node ./scripts/check-output.mjs",
    "verify": "npm run build && npm run check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.2.1"
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.10.25",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1"
  }
}
